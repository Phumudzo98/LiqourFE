import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditComplaint2Page } from './edit-complaint2.page';

describe('EditComplaint2Page', () => {
  let component: EditComplaint2Page;
  let fixture: ComponentFixture<EditComplaint2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EditComplaint2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
